package com.timesheet.Automation_Timesheet;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication(scanBasePackages = "com.timesheet")
public class AutomationTimesheetApplication {
    public static void main(String[] args) {
        SpringApplication.run(AutomationTimesheetApplication.class, args);
    }
}